\documentclass[margin=10pt]{standalone}    

\usepackage{tikz}
\usetikzlibrary{automata, positioning, arrows}

\begin{document}

\begin{tikzpicture}[node distance=2cm]
    \tikzstyle{block} = [rectangle,minimum width=2cm,minimum height=1cm,text centered,draw=black,fill=white]
    %\draw [step=0.252,gray,thin] (last.north west) grid (last.south east);


    \node (last) {$M_t$};

    \node (read) [block,right=1cm of last] {read CNN};
    \draw [->] (last) -- (read);

    \node (write) [block,below=1cm of read] {write MLP};
    \node (h) [left=1cm of write] {$h_t$};
    \node (p) [below=1cm of write] {$p_t$};

    \draw [->] (h) -- (write);
    \draw [->] (p) -- (write);
    \draw [->] (read) -- node [anchor=east]{$r_t$} (write);

    %\node (next) [right=1cm of last]{$M_{t+1}$};
    %\draw [step=0.252,gray,thin] (next.north west) grid (next.south east);

    %\draw [->] (write) -- node [anchor=east] {$w_{t+1}^{(p_t)}$} (next);

    \node (w) [right=1cm of write] {};
    \draw (write) -- (w);

    \node (cat) [right=1cm of w, minimum height=1cm] {};
    \draw [->] (w) -- (cat);
    \draw (cat.north) -- (cat.south);

    %\draw [->] (re)to [out=0,in=180]


\end{tikzpicture}

\end{document}
